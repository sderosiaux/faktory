version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - revive
    - misspell
    - unconvert
    - sqlclosecheck
    - bodyclose
    - errorlint
    - exhaustive
    - copyloopvar
    - forbidigo
  settings:
    forbidigo:
      forbid:
        - pattern: ^log\..*$
          msg: "use m.log (slog.Logger) instead of the log package"
        - pattern: ^fmt\.Print.*$
          msg: "library code must not write to stdout — use m.log or return values"
        - pattern: ^println$
          msg: "use m.log instead of println"
    gocritic: {}
    revive:
      rules:
        - name: context-as-argument
        - name: dot-imports
        - name: error-return
        - name: error-strings
        - name: increment-decrement
        - name: var-declaration
        - name: range
        - name: receiver-naming
        - name: indent-error-flow
        - name: empty-block
        - name: superfluous-else
        - name: unreachable-code
        - name: redefines-builtin-id
    errcheck:
      check-type-assertions: true
    exhaustive:
      default-signifies-exhaustive: true
  exclusions:
    rules:
      # Test files: cleanup errors are acceptable
      - path: _test\.go
        linters: [errcheck]
      # defer Close/Rollback — error checking is impractical
      - text: "Error return value of .*(Close|Rollback).*is not checked"
        linters: [errcheck]
      # Exported without comment — not a public library
      - text: "exported .* should have comment"
        linters: [revive]
      - text: "package-comments"
        linters: [revive]
      # Blank import for sqlite3 driver registration
      - text: "blank-imports"
        linters: [revive]
      # Cobra requires fixed signatures, ctx kept for API consistency
      - text: "unused-parameter"
        linters: [revive]
      # CLI and MCP server legitimately print to stdout
      - path: cmd/
        linters: [forbidigo]

formatters:
  enable:
    - gofmt
    - goimports
